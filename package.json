{
  "name": "subagents-log-service",
  "version": "1.0.0",
  "description": "Backend service that stores subagent lifecycle log data (task name, timestamps, status) in Neon Postgres, replacing the Google-Sheets-based SubLogs tracking method.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "migrate": "node server/migrate.js",
    "dev": "node --watch server/index.js"
  },
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5"
  }
}
